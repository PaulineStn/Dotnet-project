<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Gauniv.Client.ViewModel"
             x:Class="Gauniv.Client.Pages.Profile"
             x:DataType="vm:ProfileViewModel"
             Title="Profil">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12" RowDefinitions="Auto,Auto,Auto" RowSpacing="12">
                <!--
                ~1~ Photo de profil @1@
                <Border Grid.ColumnSpan="2" HorizontalOptions="Center" StrokeShape="RoundRectangle 60"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray800}}">
                    <Image WidthRequest="120" HeightRequest="120" Aspect="AspectFill"
                           Source="{Binding PhotoPath}" />
                </Border>
                <Button Grid.ColumnSpan="2" Text="Choisir une photo" Command="{Binding PickPhotoCommand}"
                        HorizontalOptions="Center" />
                        -->

                <!-- Email -->
                <Label Grid.Row="1" Grid.Column="0" VerticalTextAlignment="Center" Text="Email" />
                <Entry Grid.Row="1" Grid.Column="1" Text="{Binding Email}" Keyboard="Email"
                       Placeholder="votre@email"
                       ClearButtonVisibility="WhileEditing" />

                <!-- Mot de passe -->
                <Label Grid.Row="2" Grid.Column="0" VerticalTextAlignment="Center" Text="Mot de passe" />
                <Entry Grid.Row="2" Grid.Column="1" Text="{Binding Password}" IsPassword="True"
                       Placeholder="********" ClearButtonVisibility="WhileEditing" />
            </Grid>

            <Button Text="Enregistrer" Command="{Binding SaveCommand}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>