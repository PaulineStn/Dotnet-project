<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Gauniv.Client.ViewModel"
             x:Class="Gauniv.Client.Pages.Buy"
             Title="Buy"
             x:DataType="vm:BuyViewModel"
             x:Name="Page">

    <Grid RowDefinitions="Auto,*" Padding="16">
        <ActivityIndicator
            Grid.Row="0"
            IsVisible="{Binding IsLoading}"
            IsRunning="{Binding IsLoading}" />

        <CollectionView Grid.Row="1" ItemsSource="{Binding GamesToBuy}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:GameItemViewModel">
                    <Border Padding="12">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="{Binding Game.Name}" />
                            <Label Text="{Binding Game.Price, StringFormat='{}{0} €'}" />
                            <Button
                                Text="{Binding ActionLabel}"
                                Command="{Binding BindingContext.GameActionCommand, Source={x:Reference Page}}"
                                CommandParameter="{Binding .}" />
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>